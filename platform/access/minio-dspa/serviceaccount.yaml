# ESO uses this SA token to authenticate with the Kubernetes API when
# reading secrets from the minio namespace via the ClusterSecretStore.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: eso-minio-reader
  namespace: external-secrets-operator
