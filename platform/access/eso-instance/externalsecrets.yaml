# Deploys the external-secrets controllers into the 'external-secrets' namespace.
# The operator must be installed (eso-operator/) before this is applied.
# Sync this Application manually once the operator CSV shows Succeeded.
apiVersion: operator.openshift.io/v1alpha1
kind: ExternalSecrets
metadata:
  labels:
    app.kubernetes.io/name: external-secrets-operator
  name: cluster
  namespace: external-secrets-operator
spec: {}
