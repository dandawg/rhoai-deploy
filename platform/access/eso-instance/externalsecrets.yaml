# Deploys the external-secrets controllers (pods in the 'external-secrets' namespace).
# The operator must be installed (eso-operator/) before this is applied.
# ExternalSecretsManager (singleton) is auto-created by the operator; this CR triggers the actual deployment.
apiVersion: operator.openshift.io/v1alpha1
kind: ExternalSecretsConfig
metadata:
  labels:
    app.kubernetes.io/name: external-secrets-operator
  name: cluster
spec: {}
