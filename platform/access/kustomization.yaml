apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Entrypoint for the access-instance Argo CD Application (wave 6 in rhoai-demo-foundations).
# access-operator (wave 5) syncs eso-operator/ first so the ExternalSecrets CRD exists.
resources:
  - eso-instance
  - minio-dspa
